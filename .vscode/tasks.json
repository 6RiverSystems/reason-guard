{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "npm build",
			"type": "npm",
			"script": "build",
			// TODO: want matches for eslint & prettier too
			"problemMatcher": ["$tsc"],
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}
